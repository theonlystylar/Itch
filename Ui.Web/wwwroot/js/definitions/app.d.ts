declare class Customer {
    private name;
    constructor(name: string);
    getName(): string;
}
declare class Person {
    private _firstName;
    constructor(firstName: string);
    firstName: string;
}
